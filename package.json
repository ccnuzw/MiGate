{
  "name": "migate",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "start": "npm run dev",
    "test": "cd web && npm test",
    "check": "bash scripts/check.sh",
    "build": "make go-build"
  }
}
